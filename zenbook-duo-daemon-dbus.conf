<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <policy user="root">
    <allow own="asus.zenbook.duo"/>
    <allow send_destination="asus.zenbook.duo"/>
    <allow receive_sender="asus.zenbook.duo"/>
  </policy>

  <!--
    Fine-grained access control is enforced with Polkit in the daemon (see org.zenbook.duo.policy).
    The system bus still requires a policy entry so session processes may send to the service name.
  -->
  <policy context="default">
    <allow send_destination="asus.zenbook.duo"/>
    <allow receive_sender="asus.zenbook.duo"/>
  </policy>
</busconfig>
